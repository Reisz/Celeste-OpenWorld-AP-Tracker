name: Integration

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  checks:
    name: Run checks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout sources
        uses: actions/checkout@v5

      - name: Install tools
        uses: jdx/mise-action@v4

      - name: Download data
        run: just download

      - name: Run checks
        run: just check

      - name: Build
        run: just build
